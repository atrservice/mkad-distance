# CHANGELOG.md — история итераций проекта «МКАД Дистанс»

Формат: итерации в хронологическом порядке сверху вниз (новые сверху).
Все изменения этапа 1 внесены 15.09.2026. Версии кэша sw указаны для ориентира;
фактическое значение смотрите в sw.js (правило: инкремент при каждом деплое).

## Итерация 13 — стоимость аренды, секция «Выбор техники», переключатель режимов
- Секция «Режим» над «Маршрутом»: «Калькулятор» (активен) / «Оформить заказ» (заблокирован до введения
  регистрации; клик — тост, тултип со ссылкой на презентацию — заглушка до предоставления ссылки).
- Кнопка «Стоимость аренды» под тумблером «Возврат на МКАД»: сворачивает секцию «Маршрут»
  (в заголовке остаётся только общий километраж), прячет карту и участки, разворачивает «Выбор техники».
  Клик по свёрнутому заголовку «Маршрут» возвращает режим маршрута (вызывается map.invalidateSize()).
- Секции «Маршрут» и «Выбор техники» — аккордеон: открыта одна; свёрнутый заголовок техники
  показывает краткий выбор («Автовышка · 22 м · Шоссейное»).
- Поток выбора техники: Тип техники → тумблер «Шасси» (Шоссейное/Вездеход, по умолчанию Шоссейное;
  у «Автомобиля прикрытия» тумблера нет — список исключений) → список характеристик со своим тултипом
  (Автовышка: 16/22/28/32 м — вертикальный вылет; Кран-манипулятор: 3/5, 5/5, 7/10 — стрела/борт;
  Автокран: 16т/22м, 25т/28м, 25т/31м — грузоподъёмность/стрела; Автомобиль прикрытия: 60–130 км/ч,
  текст тултипа временный).
- Плашка стоимости: поля «За смену / За час / За 1 км» редактируемые, заполняются базовыми ценами
  по выбранному варианту и шасси; Сумма = цена за смену + Пробег × цена за 1 км (пробег из последнего
  расчёта маршрута, живое обновление). Смена характеристики/шасси сбрасывает поля на базовые.
- `js/vehicles.js`: новая модель данных (hasChassis, specName, specTooltip, options, prices по варианту
  и шасси: shift/hour/km; CHASSIS_LABELS). Базовые цены — ЗАПОЛНИТЕЛИ до таблицы пользователя (TODO в файле).
- Обработчики тултипов переведены на делегирование (динамические тултипы работают как статические;
  ссылки внутри тултипов кликабельны).
- Формула утверждена пользователем; цена за час — справочное поле (для переработок на следующих этапах).
- Задеплоено: нет (внести после проверки).
- sw: mkad-static-v21.

## Итерация 12 — тултипы: закрытие по тапу, анти-«липкий» hover (задеплоено)
- Добавлено закрытие тултипа тапом/кликом по самому сообщению (.help-tip).
- Hover-показ тултипов ограничен `@media (hover:hover)` (убран «липкий» тултип на тач-устройствах).
- Принят ВАРИАНТ А: карта остаётся Leaflet+OSM; переход на Яндекс.Карты отклонён (лицензия: бесплатный
  тариф JS API Яндекса только некоммерческий).
- sw: mkad-static-v20.

## Итерация 11 — предупреждение о недоступности сервисов (VPN)
- Новый баннер `#netWarn` (янтарный) с текстом про VPN и кнопкой закрытия.
- Триггеры: ≥3 ошибки тайлов Leaflet (tileerror), ошибка поиска адреса, ошибка маршрута.
- Код НЕ обходит VPN — только честное предупреждение пользователю.
- sw: mkad-static-v19.

## Итерация 10 — тултип тумблера и шаблонное размещение маркеров
- Текст про переключатель убран из тултипа «Маршрут» и перенесён в собственный тултип тумблера.
- Маркер «?» тумблера размещён справа от текста «Возврат на МКАД» (.toggle-row).
- На мобильных тултип = фиксированная плашка сверху экрана (не выходит за границы).
- sw: mkad-static-v18.

## Итерация 9 — блокировка возврата для внутренних адресов
- `needReturn = !lastInside && returnToggle.checked`: возврат не считается, если последний адрес внутри МКАД.
- Тумблер «Возврат на МКАД» блокируется (disabled + серый + подсказка #returnToggleHint),
  когда последний подтверждённый адрес внутри МКАД; его состояние игнорируется.
- Одиночный внутренний адрес → 0,0 км, статус «Без выезда и возврата к МКАД — 0 км».
- sw: mkad-static-v17.

## Итерация 8 — тумблер «Возврат на МКАД»
- Добавлен переключатель под адресами (по умолчанию ВКЛ): управляет возвратной ногой
  для последнего адреса снаружи МКАД.
- Статус плашки описывает комбинацию старт/возврат.
- sw: mkad-static-v16.

## Итерация 7 — маркер-подсказка «?» у заголовка «Маршрут»
- Длинный текст-инструкция убран из-под адресов в тултип по шаблону .help-wrap/.help-mark/.help-tip.
- Показ: hover (десктоп), focus, клик; закрытие: клик вне, Esc.
- sw: mkad-static-v15.

## Итерация 6 — точная геометрия МКАД из Яндекс.Карт
- Добавлен `data/mkad-yandex.txt` (MultiLineString, два кольца = внешняя и внутренняя стороны).
- Внешняя сторона определяется как кольцо большей площади; направления: внешняя против часовой,
  внутренняя по часовой.
- Точки въезда/выезда берутся с линии нужной стороны со сдвигом вдоль кольца (старт вверх по ходу,
  финиш вниз по ходу) + bearings направления стороны.
- Убрана служебная строка «Схема МКАД…» из UI (диагностика в console).
- sw: mkad-static-v14.

## Итерация 5 — финиш без проезда съезда
- Два кандидата точки финиша (-0.3 и +0.5 км вдоль стороны) считаются параллельно, берётся короткий маршрут.
- loop-guard: дорожная дистанция > прямой + 4 км → кандидат 0.9 км.
- Маркеры МКАД рисуются по фактическим концам геометрии маршрута.
- sw: mkad-static-v13.

## Итерация 4 — съезды как узлы рамп
- Overpass-запрос тянет way «МКАД» + примыкающие *_link; узлы примыкания = съезды;
  сторона узла по близости к линии внешней/внутренней стороны.
- Ручные съезды `MKAD_JUNCTIONS_OVERRIDE` (config.js) сливаются с парсеными (приоритет ручных).
- Добавлен `junction-collector.html` — кликер карты для сбора координат съездов.
- sw: mkad-static-v12.

## Итерация 3 — Dadata и ближайшие города в подсказках
- Подсказки через Dadata (Москва + МО параллельно; МО сортируется по близости к центру Москвы),
  добивка Nominatim ≤800 мс; дедупликация <120 м; лимит 10.
- Пустой `DADATA_TOKEN` = прежнее поведение на Nominatim.
- sw: mkad-static-v11.

## Итерация 2 — бизнес-правила «внутри МКАД = 0 км»
- needStart = первый адрес снаружи; needReturn (тогда) = последний адрес снаружи.
- Матрица маршрутов зафиксирована (см. PROMPT.md раздел 8).
- sw: mkad-static-v10.

## Итерация 1 — MVP калькулятора
- Динамические строки адресов с ghost-строкой; подсказки Nominatim; ввод координат с reverse-геокодингом.
- Автоматический пересчёт (дебаунс 250 мс), плашка пробега, список участков, карта Leaflet.
- МКАД: Overpass → Nominatim → км-кольцо 108 точек (фолбэк); bearings и сдвиги против петель OSRM.
- PWA: manifest, sw, иконки; мобильная адаптация (плашка fixed, kb-open).
- Деплой на GitHub Pages (atrservice/mkad-distance).